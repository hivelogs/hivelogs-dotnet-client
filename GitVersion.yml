branches:
  main:
    regex: ^main$
    label: ""
    increment: Minor
    track-merge-target: false
    is-mainline: true
  stage:
    regex: ^stage$
    label: preview
    increment: Patch
    track-merge-target: false
ignore:
  sha: []
